package org.example.simulation.intersection;

/**
 * Helper enum to determine a lane the car should be on,
 * Either LEFT lane, or STRAIGHT_OR_RIGHT lane
 */
public enum TurnType {
  LEFT,
  RIGHT,
  STRAIGHT
}
